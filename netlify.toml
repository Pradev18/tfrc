[build]
  command = "npm run netlify:build"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--legacy-peer-deps"

# Bundle read-only SQLite for serverless functions (catalogue browsing)
[functions]
  included_files = ["prisma/prod.db"]

[[plugins]]
  package = "@netlify/plugin-nextjs"
